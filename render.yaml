services:
  - type: web
    name: naqaab-backend
    env: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: naqaab
      - key: CORS_ORIGINS
        value: https://naqaab-frontend.onrender.com,https://filmmaking.onrender.com

  - type: static
    name: naqaab-frontend
    rootDir: frontend
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://naqaab-backend.onrender.com
